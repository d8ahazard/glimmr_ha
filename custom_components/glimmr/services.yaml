effect:
  name: Set effect
  description: Control the effect settings of GLIMMR.
  target:
    entity:
      integration: glimmr
      domain: light
  fields:
    effect:
      name: Effect
      description: Name or ID of the GLIMMR light effect.
      example: "Rainbow"
      selector:
        text:
    
update:
  # Description of the update state service
  description: Trigger for update the state of the Glimmr device.